#include <iostream>
#include <fstream>
using namespace std;

int main() {

    ofstream out("alpha.txt");

    for(char ch = 'A'; ch <= 'Z'; ch++) {
        out << ch;
    }

    out.close();

    ifstream in("alpha.txt");

    in.seekg(9);

    char ch;

    in.get(ch);

    cout << "10th Character = " << ch;

    in.close();

    return 0;
}
